spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sep
    username: root
    password: 12345
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8085 # Set the port number you want to run your application on

# Additional configuration properties go here
